class Collection
{
    void LearnList()
        {
            List<byte> numbers = new List<byte>();
            numbers.Add(23);
            numbers.Add(237);
            numbers.Add(239);
            numbers.Remove(239);
        }   
}